<!DOCTYPE html>
<html>
  <head>
    <title>Generic Partner Example App</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1>Users</h1>
    <% users.forEach((user) => { %>
      <div>
        <p><%= user.firstName %> <%= user.lastName %></p>
        <pre><%= JSON.stringify(user, null, 2) %></pre>
        <a href="/login/<%= user.id %>">Login as this user</a>
      </div>
    <% }) %>
  </body>
</html>
